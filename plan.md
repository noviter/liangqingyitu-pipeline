# 两清一图自动化产品计划（第一版重构）

## 1. 文档目的

本计划以《处室板块视角两清一图梳理》中的人工成功路径为唯一业务基线，规划“两清一图”的第一版自动化实现。

第一版要实现的不是重新定义梳理方法，而是把已经被人工验证可接受的三段 Prompt、Excel 成果格式和 Draw.io 绘图规范，整理成可重复运行、可逐阶段审核、可供团队安装使用的产品能力。

业务人员审核和阶段交接的正式产物必须是可直接打开、阅读、批注和修改的 Excel 与 Draw.io/JPG 文件。JSON 仅可作为程序或大模型在运行时的内部临时数据，不作为中间交付物、不发送给甲方业务人员、不要求业务人员理解。

## 2. 已确认的业务基线

本项目不从零创造新的业务 Skill，直接以 PDF 中已跑通的人工流程为基础进行工程化改造。

| 人工步骤 | PDF 中的基础 | 第一版自动化方式 |
|---|---|---|
| 职责拆解与职能分类 | Prompt1：三定方案业务拆解与分类 | 固化为阶段 1 Skill，输入职责原文，输出事项拆解审核 Excel。 |
| 事项清单整理 | Prompt2：阶段成果 Excel 整理 | 固化为阶段 1 的 Excel 生成部分，严格按四列、合并和黑白灰样式输出。 |
| 数字化与流程梳理 | 第二部分“可复用的数字化判断与流程梳理指令” | 固化为阶段 2 Skill，读取审核后的事项表与系统资料，输出 E-I 列完整的 Excel 中间产物。 |
| 泳道图绘制 | Prompt3：泳道图绘制 | 固化为阶段 3 Skill，读取阶段 2 审核后的 Excel，输出每事项一页的 Draw.io 文件和图像。 |

对原 Prompt 的改进只限于工程化需要：参数化输入路径、自动读取资料、自动生成 Excel/Draw.io、保存版本、记录人工修改、校验输出完整性。不得改变人工已经确认的业务规则、字段语义、事项粒度、职能分类口径和图形规范。

## 3. 产品目标

### 3.1 第一版目标

1. 新处室只提供职责原文和规定范围内的系统资料时，能够独立生成“两清一图”初稿。
2. 每个业务阶段都产生一份面向业务人员的 Excel 或图形成果，供人工审核后再进入下一阶段。
3. 生成结果严格遵循 PDF 中的 Prompt 和已接受的人工 Excel/Draw.io 格式。
4. 人工审核后的文件成为下一阶段唯一输入，自动任务不得覆盖人工修改。
5. 人工样例只用于校验规则、确认版式和盲跑评估，不能被复制为新处室的答案。
6. 第一版稳定后，将三项业务 Skill、脚本和模板迁移到独立仓库，供团队成员安装和使用。

### 3.2 非目标

- 不把 JSON、事项 ID、证据 ID、模型置信度等技术字段放入甲方审核 Excel 的可见列。
- 不为新处室复制人工样例中的事项、数字化结论、流程依据或泳道图。
- 不把资料未覆盖直接判定为“无数字化”。
- 不为追求流程完整而虚构主体、审批权限、退回路径或系统操作。
- 第一版只处理“两清一图”；“三清两图”的第三清和第二图在定义明确前不预设实现。

## 4. 业务人员可见的阶段成果

三大阶段全部采用可读、可修改的业务成果文件。运行时的内部结构化数据可以存在于临时工作目录，但不属于业务中间产物，也不进入交付目录。

### 4.1 阶段 1：业务事项拆解

**业务审核文件：** `01_处室名称_三定方案业务事项清单_人工核验前.xlsx`

此文件遵循 PDF Prompt2 的四列成果格式；不显示技术字段：

| 列 | 字段 | 业务含义 |
|---|---|---|
| A | 处室名称 | 来源 Excel 中的处室名称。 |
| B | 职责 | 三定方案完整职责原文，保留条款序号，不概括、不改写。 |
| C | 事项 | 按 Prompt1 原子化拆出的完整业务事项，保留动词前缀、业务对象和责任范围。 |
| D | 职能类型 | 督导、组织、负责、参与之一。 |

文件格式严格沿用 PDF 要求：同处室 A 列纵向合并，同来源职责 B 列纵向合并，C/D 不合并；灰色表头、黑白灰正文、职责分组交替底色、清晰边框、自动换行、冻结首行。

**人工审核动作：** 业务人员在此表中确认事项是否漏拆、过拆、越权改写、分类错误，及是否需要排除“领导交办的其他事项”等明确范围外条目。

**审核后文件：** `01_处室名称_三定方案业务事项清单_核验后.xlsx`

审核后文件保持四列格式，是阶段 2 的正式输入。若需要记录审核意见，另附同名 `审核说明.docx` 或 `审核记录.xlsx`，不在四列表内增加事项 ID 或技术列。

### 4.2 阶段 2：数字化支撑与业务流程梳理

**业务审核文件：** `02_处室名称_数字化与业务流程_人工核验前.xlsx`

以阶段 1 已审核的四列为基础，按 PDF 第二部分的结构增加业务字段。若甲方已经确认现有总表的列顺序、列名或“待核验问题”列，输出必须以该确认模板为准；程序按字段名称识别，不依赖固定列号。

PDF 默认字段语义如下：

| 字段 | 业务含义与填写规则 |
|---|---|
| A-D | 保留阶段 1 已审核的处室、职责、事项、职能类型及其合并关系，不得擅自拆分、合并或重排。 |
| E 有没有数字化 | 仅填写“有、待确认、无、不纳入本次梳理范畴”。“有”表示系统可支撑事项或明确主要分支的大部分办理过程；资料缺失不能直接填“无”。 |
| F 流程/待确认问题 | 使用“流程：……；待确认：……”的连贯业务叙述。说明谁办理、处理什么材料、交给谁、如何反馈、形成什么成果。资料不足时使用“流程线索：……；待确认：……”。 |
| G 泳道图 | 本阶段留空。阶段 3 核验后在此列放入导出的 JPG/PNG 图像或图像链接。 |
| H 系统（标号） | 仅列与事项存在直接或较强支撑关系的系统全称及编号；多个系统用中文分号分隔。外部渠道注明未列入本次系统清单，不编造编号。 |
| I 流程依据 | 按节点说明办理主体与业务动作、文件全称、章节/页码、系统支持的功能/材料/状态，以及形成或改变的结果。区分直接依据、业务逻辑整理、人工补充和待核验内容。 |

第二阶段 Excel 是“流程依据梳理结果”的正式保存载体。流程依据不另以 JSON 或纯技术检索清单交给业务人员；业务人员可直接在 I 列核对证据，并在 F 列审核流程表述。

**人工审核动作：** 业务人员可调整事项合并、数字化结论、主责单位、系统名称、正常或异常路径及待确认问题。系统不在目录内但实际使用时，可在 H/I 列补充来源和说明。

**审核后文件：** `02_处室名称_数字化与业务流程_核验后.xlsx`

该文件是阶段 3 唯一输入。收到人工核验后文件时，程序只能读取和继承其内容，不得用旧初稿或原规则覆盖人工修改。

### 4.3 阶段 3：数字化业务事项泳道图

**业务审核文件：**

- `03_处室名称.drawio`：每个有数字化的事项一个页签；
- `03_处室名称_泳道图核验版.xlsx`：基于阶段 2 核验后表，在 G 列放入导出的 JPG/PNG；
- `03_处室名称_泳道图待核验清单.xlsx`：仅列无法安全出图或图中仍需确认的事项及原因。

阶段 3 严格按 PDF Prompt3 执行：

- 输入为事项、所属处室、职责、流程依据、有没有数字化、历史图（如存在）、系统名称/编号、待核实问题和备注；
- 系统只能作为节点支撑说明，不能成为泳道主体；
- 每事项一页，页签命名为“行xx-事项名称”，标题为“业务事项：事项名称”；
- 黑白灰风格，开始为细线圆圈，结束为加粗圆圈，活动为圆角矩形，判断为菱形；
- 有依据的退回补正必须标出退回对象和回流节点；没有依据的内容放入待核验说明，不得补造；
- 每页必须有明确结果和结束节点，系统支撑应隐藏在业务流程之后。

**人工审核动作：** 核对泳道主体、业务顺序、判断分支、退回路径、结果节点和图面排版；人工调整后的 Draw.io 和导出图片作为最终成果。

## 5. 三项业务 Skill

第一版只建设和发布以下三项业务 Skill。资料读取、文档提取、Excel 格式化、Draw.io 渲染等为它们调用的脚本能力，不单独包装成业务 Skill，不要求业务人员理解。

### 5.1 Skill 一：三定方案业务拆解与清单整理

**基础：** PDF Prompt1 + Prompt2。

**职责：**

1. 读取职责 Excel 的第一个工作表中的单位、处室和职责原文；
2. 按 Prompt1 的原子化拆解和职能类型映射规则生成事项；
3. 将结果按 Prompt2 生成四列 Excel；
4. 输出人工核验前和人工核验后的阶段 1 文件，不改变已审核版本。

**必须保留的 Prompt1 规则：**

- 识别“参与拟订、承担指导、负责指导”等完整复合动词；
- 并列动作必须拆分，子项保留完整动词前缀；
- 固定政务名词、完整动作闭环不得机械拆分；
- 按原文顺序输出，不合并、不去重、不打乱；
- 先判断复合动词再分类；“组织实施/组织开展”为负责，“组织协调/统筹协调”为组织，“监督实施/监督执行”为督导，“参与拟订/参与编制”为参与；
- 无法明确对应或分类时提示具体问题，不能猜测补齐。

**业务输入：** 原始职责 Excel、明确的纳入/排除范围、输出目录和文件名。

**业务输出：** 阶段 1 的两份四列 Excel 文件和简短统计说明。

### 5.2 Skill 二：数字化支撑与业务流程梳理

**基础：** PDF 第二部分“可复用的数字化判断与流程梳理指令”。

**职责：**

1. 读取阶段 1 核验后 Excel 的每一条事项；
2. 在指定系统资料、制度、操作实例和人工补充材料中查找流程依据；
3. 逐条填写数字化判断、流程/待确认问题、系统（标号）和流程依据；
4. 生成阶段 2 人工核验前 Excel，并尊重人工审核后的版本。

**必须保留的判断口径：**

- 事项对象和系统处理对象、适用范围一致后，才可关联；不能因关键词相同而将高速、普通国省干线、农村公路、道路运输流程混用；
- “有”表示主要业务流程得到支撑，不要求岗位、退回路径等每个细节都明确；
- 只有零散功能、背景或数据展示时为“待确认”；资料未覆盖不等于现实没有数字化；
- “无”必须有材料或人工意见支持；“不纳入本次梳理范畴”必须有范围依据；
- 数据表可证明字段和关联，不能单独证明角色、审批和状态转换；
- “保存成功”“上报确认提示”“审核通过”分别描述；
- 已有证据的正常流程直接写；退回、补正、取消、复核、归档没有证据时写入待确认；
- 处室的建议性工作必须标注“建议角色/待人工核验”，不得伪装为系统文件直接规定。

**业务输入：** 阶段 1 核验后 Excel、系统资料目录、系统名称与编号、补充制度/办事指南/人工意见、业务范围和特殊规则。

**业务输出：** 阶段 2 的人工核验前 Excel、统计说明；人工确认后保存核验后 Excel。

### 5.3 Skill 三：业务泳道图绘制

**基础：** PDF Prompt3。

**职责：**

1. 读取阶段 2 核验后 Excel 中数字化结论为“有”的事项；
2. 综合事项、职责、F 列流程、I 列流程依据、H 列系统、待确认内容和历史图；
3. 生成一个处室一个 Draw.io 文件、每事项一个页签的泳道图；
4. 导出 JPG/PNG，写回阶段 3 核验版 Excel 的 G 列；
5. 将证据不足或主体不明的事项列入待核验清单，而不是强行画图。

**业务输入：** 阶段 2 核验后 Excel、已有泳道图（仅作版式与历史参考）、输出目录。

**业务输出：** Draw.io、含图像的 Excel、待核验清单。

## 6. 自动化运行方式

### 6.1 一次运行的业务目录

```text
运行包_YYYYMMDD_处室名称/
  输入资料清单.xlsx
  01_处室名称_三定方案业务事项清单_人工核验前.xlsx
  01_处室名称_三定方案业务事项清单_核验后.xlsx
  02_处室名称_数字化与业务流程_人工核验前.xlsx
  02_处室名称_数字化与业务流程_核验后.xlsx
  03_处室名称.drawio
  03_处室名称_泳道图核验版.xlsx
  03_处室名称_泳道图待核验清单.xlsx
  审核说明/
```

`输入资料清单.xlsx` 面向业务人员展示本次使用的职责文件、系统资料、补充材料、系统范围、排除范围和运行日期；不显示技术摘要或内部 ID。

### 6.2 内部运行数据原则

- 程序可将 Excel 表格解析为内部结构化数据，以便大模型准确读取、脚本进行合并单元格处理和避免因行号变化错位。
- 内部数据存放在运行目录的隐藏或技术子目录，不作为业务成果和审核对象。
- 人工 Excel 始终是每一阶段的权威输入与输出；自动化读取人工核验后表时，以表中内容为准。
- 不把内部事项 ID、证据 ID、模型分数、原始检索候选写入阶段成果的可见列。需要追溯时，由实施人员通过脚本和运行日志查询，而不是要求甲方业务人员阅读 JSON。

### 6.3 新处室盲跑规则

对新处室或验证处室执行时，生成链路只允许读取：职责原文、阶段 1/2 已审核文件、指定系统资料、补充制度或人工意见。

以下材料只能在结果生成后用于比较，不能进入 Prompt 或脚本的生成输入：

- 该处室的历史人工事项答案；
- 该处室的人工数字化结论、流程和流程依据；
- 该处室已有 Draw.io 图的业务内容。

人工样例可用于确认 Excel 外观、Draw.io 样式和最终差异分析，但不能成为“抄答案”的来源。

## 7. Excel 输出与人工审核规范

### 7.1 格式优先级

输出格式按以下优先级确定：

1. 甲方已经确认并正在使用的人工 Excel 模板；
2. PDF Prompt2 和第二部分明确规定的字段、合并、样式和校验要求；
3. 未被规定的细节才采用统一黑白灰、边框、自动换行、合适列宽行高和冻结首行。

程序必须按表头语义识别字段，不能假定某一列永远固定在 C、D、F 或 I；这样既能兼容 PDF 默认表，也能兼容当前人工总表中已确认的列顺序差异。

### 7.2 人工审核方式

- 业务人员直接在 Excel 中修改事项、结论、流程和依据；不需要接触 JSON、命令行或代码。
- 对无法确认的问题，在现有“待核验问题”或 F 列“待确认”段落中用业务语言注明缺少什么：主体、材料、权限、顺序、返回节点、结果或系统适用范围。
- 审核后文件另存版本，不覆盖人工核验前文件。
- 下一阶段只能读取指定的核验后 Excel；若没有核验后文件，流程停止在该阶段，不自动将初稿当作已确认结论。

## 8. 技术实现范围

技术实现服务于三项既定 Skill，不增设额外业务概念。

| 技术组件 | 服务对象 | 职责 |
|---|---|---|
| 输入读取脚本 | Skill 一、二 | 只读职责 Excel 第一个 sheet；识别单位、处室、职责文本；建立可读输入清单。 |
| 系统资料文本提取与检索脚本 | Skill 二 | 将 PDF/DOCX 等资料转换为可检索文本，并保留文件名、章节或页码，用于填写 I 列流程依据。 |
| Excel 生成与校验脚本 | Skill 一、二、三 | 按人工模板渲染、合并单元格、保留格式、检查必填列、冻结首行并导出。 |
| Draw.io 生成与预览脚本 | Skill 三 | 按 Prompt3 将审核后流程转换为多页 Draw.io，并导出图像和检查图面。 |
| 版本与差异记录脚本 | 全流程 | 保存人工核验前后版本，记录本次运行使用的资料和输出位置。 |

当前已完成的资料扫描和文档提取能力可保留；当前“人工样例映射生成”只能留作格式检查或回归比对工具，必须从正式运行链路移除。

## 9. 本周实施顺序

### 工作包 1：以人工模板为准重建阶段 1

1. 从 PDF Prompt1、Prompt2 提取并固化已有拆解、分类、四列 Excel 和样式规则。
2. 修复 Excel 读取中“流程”和“流程依据”表头混淆的问题，但本工作包不使用阶段 2 内容生成答案。
3. 实现输入职责 Excel 只读取第一个 sheet。
4. 用一个处室独立运行 Prompt1 逻辑，生成四列 `01_人工核验前.xlsx`，不读取该处室人工答案。
5. 人工在 Excel 中审核，形成 `01_核验后.xlsx`。

验收：业务人员可直接审核 Excel；所有事项能在 B 列回溯到原职责；没有可见技术 ID；输出格式与人工样例一致。

### 工作包 2：以 PDF 第二部分为准重建阶段 2

1. 输入阶段 1 核验后 Excel 和系统资料目录。
2. 对每一事项检索资料并在 I 列写出文件、章节/页码、功能/材料/状态和支撑节点。
3. 依据 PDF 口径填写 E、F、H、I，G 列保持为空。
4. 生成 `02_人工核验前.xlsx`，由业务人员逐条审核后另存 `02_核验后.xlsx`。

验收：I 列不再为空；每条“有数字化”均能在 I 列找到可回溯的材料依据；F 列使用业务叙述而非技术节点表；资料不足时明确为待确认。

### 工作包 3：以 Prompt3 为准重建阶段 3

1. 仅读取阶段 2 核验后 Excel 中“有数字化”的事项。
2. 根据 F/I/H 列和已确认主体生成 Draw.io 页面。
3. 导出图像写入 G 列，另存 `03_泳道图核验版.xlsx`。
4. 对主体、路径或结果不明确的事项输出待核验清单，保留到下一轮处理。

验收：每个出图事项一页；系统不是泳道；有明确开始、结果和结束；图像可在 Excel 中查看，Draw.io 可继续人工编辑。

### 工作包 4：端到端盲跑与迭代

1. 选择一个未用于规则调试的处室。
2. 禁止读取该处室人工样例的业务内容，独立执行三项 Skill。
3. 人工对每个阶段 Excel 审核，并记录事项、数字化、依据、主体和图形差异。
4. 只将可泛化的差异修正为 Prompt、模板或脚本规则，不写入该处室的固定答案。
5. 保存每轮运行包，形成可比较的迭代记录。

验收：从原始职责和系统资料开始，能够生成三个阶段的业务可读成果；人工可以逐阶段修订，且下一轮不会覆盖人工修订。

## 10. 质量门槛

| 阶段 | 自动检查 | 人工确认重点 |
|---|---|---|
| 阶段 1 | A-D 无空分类；事项数量与职责关联完整；合并范围、顺序、样式正确。 | 事项粒度、职责边界、职能类型、纳入/排除范围。 |
| 阶段 2 | A-D 未被擅改；E 值合法；G 为空；H 系统编号来源明确；I 列非空的“有”事项可定位文件。 | 数字化结论、流程主体、步骤、待确认问题、系统适用范围、流程依据真实性。 |
| 阶段 3 | 仅对“有”事项出图；每页标题、泳道、开始、结果、结束、判断分支和图像导出完整。 | 主体是否准确、流程是否真实、退回路径是否正确、图面是否可用。 |

## 11. 产品形态：可迁移、可解耦的 Skill 工作流

当前目录用于打磨；待三项 Skill 通过盲跑验证后，建设独立仓库。产品核心不是“只能安装在 Codex 的 Skill”，而是一套可本地下载、可版本管理、可协作、可复跑的“两清一图”工作流。Codex 是第一种适配方式，不是唯一运行环境。

### 11.1 四层产品结构

```text
业务方法层
  三项中文 Skill、PDF Prompt 规则、审核口径、Excel/Draw.io 模板
        ↓
通用执行层
  职责读取、系统资料提取与检索、Excel 生成、Draw.io 生成、版本保存脚本
        ↓
阶段编排层
  阶段 1 / 阶段 2 / 阶段 3 独立运行，以及带人工闸口的全流程运行
        ↓
平台适配层
  Codex 适配包、其他 Agent 工作台适配包、通用本地命令行入口
```

业务方法、模板和阶段 Excel 成果不绑定任何一个 Agent 工作台。平台适配层只负责把同一项业务 Skill 调用到特定工作台中，不能复制或改写业务规则。

### 11.2 可独立运行，也可全流程编排

三项 Skill 必须可以各自运行：

| 运行方式 | 正式输入 | 正式输出 |
|---|---|---|
| 阶段 1 单独运行 | 职责原文 Excel 与纳入/排除范围 | 四列事项清单 Excel。 |
| 阶段 2 单独运行 | 阶段 1 核验后 Excel、系统资料和补充材料 | 数字化与流程 Excel。 |
| 阶段 3 单独运行 | 阶段 2 核验后 Excel | Draw.io、含图像的 Excel、待核验清单。 |
| 全流程运行 | 职责原文、系统资料和范围规则 | 顺序执行三阶段，并在每份核验后 Excel 到位前暂停。 |

全流程不是另一个“大 Prompt”，而是顺序调用三项独立 Skill。阶段间只通过核验后的 Excel 和 Draw.io 成果交接，不依赖某个 Agent 的对话记忆或 Excel 行号。因此换人、换电脑、换 Agent 工作台或隔期继续处理时，均可从最近一份核验后 Excel 接续。

### 11.3 Agent 工作台兼容原则

不同工作台对“Skill”、文件访问、脚本执行和模型调用的支持不同，因此不承诺复制一个 `SKILL.md` 后可在所有平台无差别运行。仓库应提供同一业务能力的多种适配入口：

- Codex：使用平台所需的 Skill 文件和本地工具调用；
- 其他支持工作流或 Prompt 包的 Agent 工作台：使用同一份中文 Prompt、模板和脚本说明；
- 仅支持本地命令的环境：使用通用命令行入口生成 Excel/Draw.io，再由人工或 Agent 审核；
- 不具备本地文件或脚本权限的环境：可以使用三项中文 Prompt 和模板进行半自动处理，但不承诺完整自动化。

无论使用哪种环境，业务人员看到的阶段成果、字段语义和审核方式必须一致。

### 11.4 独立仓库结构

```text
liangqingyitu/
  skills/                         # 三项业务 Skill 的通用中文原稿
    三定方案业务拆解与清单整理/
    数字化支撑与业务流程梳理/
    业务泳道图绘制/
  workflows/                      # 单阶段与全流程编排说明
    阶段1.md
    阶段2.md
    阶段3.md
    全流程.md
  scripts/                        # 解析、检索、Excel/Draw.io 生成与校验
  templates/
    阶段1事项清单模板.xlsx
    阶段2数字化与流程模板.xlsx
    阶段3泳道图模板.drawio
  references/
    职能分类口径.md
    流程依据填写口径.md
    泳道图绘制规范.md
  adapters/                       # 仅负责平台调用差异
    codex/
    other-agent-workbenches/
    local-cli/
  examples/                       # 脱敏输入、预期 Excel 和 Draw.io 成果
  tests/                          # 阶段测试、端到端盲跑和回归比对
  README.md
  CHANGELOG.md
```

`skills/` 是业务方法的单一事实来源；`adapters/` 不保存另一套业务 Prompt。模板、脚本和示例均以相同版本号发布，避免使用者安装了新 Skill 却仍使用旧 Excel 模板。

### 11.5 团队协作与安装使用

仓库 README 面向团队成员说明：如何下载或克隆仓库、如何安装适配其工作台的入口、如何准备职责和系统资料、如何单独运行某个阶段、如何启动全流程、每阶段审核什么、如何提交核验后 Excel 继续执行。

README 不要求用户理解内部 JSON、索引或模型参数。业务同事的操作应收敛为“准备输入 Excel/资料 -> 获取本阶段审核 Excel -> 另存核验后版本 -> 继续下一阶段”。

发布前需要完成：版本号、变更记录、模板版本、安装方式、运行环境、依赖清单、样例脱敏策略和资料合规检查。对外发布前必须确认项目授权范围。

## 12. 向“三清两图”扩展

第一版只实现“两清一图”：职责/事项梳理、数字化与流程梳理、数字化业务泳道图。

后续扩展到“三清两图”时，先由业务方确认第三清、第二图的正式定义、字段、审核格式和图形规则，再基于相同模式增加相应 Excel 审核成果和 Skill。不得从当前处室样例自行推断第三清或第二图含义。

## 13. 当前结论与下一步

1. 甲方业务人员看到、审核、修改和保存的始终是 Excel 与 Draw.io/JPG，不是 JSON。
2. 三项业务 Skill 必须直接复用 PDF 的 Prompt1/Prompt2、第二部分流程 Prompt 和 Prompt3，只做参数化与工程化改进。
3. 阶段 2 的 I 列“流程依据”是必须保存、必须审核的正式中间成果；阶段 3 以阶段 2 核验后 Excel 为唯一业务输入。
4. 当前人工样例映射版只保留为格式检查与盲跑评估工具，不进入正式生成流程。
5. 下一步从工作包 1 开始，先重建阶段 1 的独立职责拆解和四列 Excel 输出，再逐步推进阶段 2 和阶段 3。
